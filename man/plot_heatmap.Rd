% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/04_fash.R
\name{plot_heatmap}
\alias{plot_heatmap}
\title{Heatmap Plot of Posterior Weights for FASH Objects}
\usage{
plot_heatmap(
  object,
  selected_indices = NULL,
  size_range = c(1, 8),
  size_breaks = NULL,
  font_size = 10,
  ...
)
}
\arguments{
\item{object}{A \code{fash} object containing posterior weights.}

\item{selected_indices}{Optional character vector of dataset names or numeric indices
to specify which rows (datasets) to display. Default is \code{NULL} (all datasets).}

\item{size_range}{A numeric vector of length 2 specifying the range of point sizes. Default is \code{c(1, 8)}.}

\item{size_breaks}{A numeric vector specifying size breaks from 0.1 to 0.9.
Default is \code{NULL}, which automatically selects a set of breaks.}

\item{font_size}{A numeric value specifying the base font size for theme elements. Default is \code{10}.}

\item{...}{Additional arguments passed to \code{ggplot2::theme} or \code{ggplot2::geom_point}.}
}
\value{
A \code{ggplot} object representing the heatmap plot of posterior weights.
}
\description{
This function generates a heatmap plot visualizing the posterior weights from a \code{fash} object.
The y-axis shows dataset names, the x-axis shows PSD grid values, and point sizes
represent the posterior weights.
}
\examples{
# Simulate example
data_list <- lapply(1:10, function(i) data.frame(y = rpois(16, 5), x = 1:16, offset = 0))
grid <- seq(0, 2, length.out = 6)
fash_obj <- fash(data_list = data_list, Y = "y", smooth_var = "x", grid = grid, likelihood = "poisson")

# Heatmap plot for all datasets
plot_heatmap(fash_obj)

# Subset some datasets
plot_heatmap(fash_obj, selected_indices = 1:5)

}
