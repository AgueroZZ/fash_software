% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/01_likelihood.R
\name{compute_L_gaussian_helper_seq}
\alias{compute_L_gaussian_helper_seq}
\title{Compute Log-Likelihoods for All Grid Values of PSD (Predictive Standard Deviation)}
\usage{
compute_L_gaussian_helper_seq(
  data_i,
  Si,
  Omegai,
  grid,
  num_basis = 30,
  betaprec = 1e-06,
  order = 2,
  pred_step = 1
)
}
\arguments{
\item{data_i}{A single dataset extracted from the \code{data_list} component of \code{fash_set_data}.
Must be a list containing \code{y}, \code{x}, and \code{offset}.}

\item{Si}{A numeric vector representing the standard errors for the dataset.}

\item{Omegai}{A numeric precision matrix for the dataset.}

\item{grid}{A numeric vector representing the grid of PSD (Predictive Standard Deviation) values.}

\item{num_basis}{An integer specifying the number of O-Spline basis functions.}

\item{betaprec}{A numeric value representing the precision of the fixed effects coefficients.}

\item{order}{An integer specifying the order of the Integrated Wiener Process (IWP) prior.}

\item{pred_step}{A numeric value specifying the prediction step size.}
}
\value{
A numeric vector of log-likelihood values, one for each grid value.
}
\description{
Computes the log-likelihood for a single dataset across all values of \code{psd_iwp} specified in the grid.
This function iteratively calls \code{compute_L_gaussian_helper} for each grid value.
}
\examples{
# Example usage
set.seed(1)
Y <- matrix(rnorm(20), nrow = 4, ncol = 5)
smooth_var <- matrix(runif(20), nrow = 4, ncol = 5)
offset <- 1
S <- c(0.5, 0.8, 1.2, 1.0, 0.9)
Omega <- diag(5)
data <- fash_set_data(Y, smooth_var, offset, S, Omega)
grid <- seq(0.1, 2, length.out = 10)
likelihoods <- compute_L_gaussian_helper_seq(
  data$data_list[[1]], Si = S[[1]], Omegai = Omega, grid = grid
)

}
\keyword{internal}
